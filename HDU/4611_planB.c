#include <stdio.h>

int main()
{
    int pa, pb, A, B, i, ThisCaseRoundScore, score, times;
    scanf( "%d", &times );
    while( times-- )
    {
        scanf( "%d %d %d", &N, &A, &B );
        for( i = 0; i < B; i++ )
        {

        }
    }
}